# ==========================================
# CONFIGURAÇÕES GERAIS
# ==========================================
ENV=dev  # dev, staging, production
DEBUG_MODE=true
APP_NAME=TD Base Python Automations

# ==========================================
# DATABASE - PostgreSQL
# ==========================================
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=sua_senha_aqui
DB_NAME=tdax

# URLs customizadas (opcional - sobrescreve as construídas automaticamente)
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
# DATABASE_URL_TDAX=postgresql://user:pass@host:5432/tdax
# DATABASE_URL_AUTOMATION=postgresql://user:pass@host:5432/automacoes

# ==========================================
# AWS
# ==========================================
AWS_ACCESS_KEY_ID=sua_access_key
AWS_SECRET_ACCESS_KEY=sua_secret_key
AWS_REGION=us-east-1
AWS_S3_BUCKET=meu-bucket

# ==========================================
# LOGGER
# ==========================================
LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FORMAT=default  # default, json, simple

# CloudWatch (usado apenas se ENV != dev)
CLOUDWATCH_LOG_GROUP=/aws/td-automacoes
CLOUDWATCH_REGION=us-east-1
# Nota: log_stream_name é gerado automaticamente como: {app_name}-{env}-{DDMMYYYY}

# ==========================================
# RABBITMQ (Condicional)
# ==========================================
RABBIT_USAGE=false  # Se true, as variáveis abaixo são obrigatórias

# Obrigatórias apenas se RABBIT_USAGE=true
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=guest
RABBITMQ_PASSWORD=guest
RABBITMQ_VHOST=/

# ==========================================
# NOTAS IMPORTANTES
# ==========================================
# 
# 1. DEBUG_MODE:
#    - Se true: força LOG_LEVEL=DEBUG e exibe logs detalhados
#    - Se false: usa LOG_LEVEL configurado
#
# 2. Logs (baseado em ENV):
#    - ENV=dev: logs apenas no console (local)
#    - ENV=staging/production: logs no console + CloudWatch (se configurado)
#
# 3. Log Stream Name (gerado automaticamente):
#    - Formato: {app_name}-{env}-{data}
#    - Exemplo: td-app-production-29102025
#
# 4. RabbitMQ (condicional):
#    - RABBIT_USAGE=false: variáveis do RabbitMQ são opcionais
#    - RABBIT_USAGE=true: variáveis do RabbitMQ são obrigatórias
#
# 5. Database URLs:
#    - Se não definir DATABASE_URL_*, URLs são construídas automaticamente
#    - Formato: postgresql://{user}:{password}@{host}:{port}/{database}
#
